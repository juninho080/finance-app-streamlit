{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Streamlit: run app",
      "type": "debugpy",                // ← novo tipo
      "request": "launch",
      "module": "streamlit",
      "args": [
        "run",
        "${workspaceFolder}/finance_app_streamlit.py",
        "--server.headless=true"
      ],
      "console": "integratedTerminal",
      "justMyCode": false               // depura libs se precisar
    }
  ]
}
